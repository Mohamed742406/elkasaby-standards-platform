# 📚 منصة محمد القصبى للمواصفات الهندسية - التوثيق الشامل

## نظرة عامة

منصة متخصصة لرفع وإدارة وتحميل المواصفات الهندسية المترجمة (ASTM, ACI, BS). تتميز بنظام أمان متقدم يسمح للمالك بإدارة الملفات بينما يتمتع الزوار بإمكانية البحث والتحميل.

---

## ✨ الميزات الرئيسية

### 1. **نظام المصادقة الآمن**
- تسجيل دخول بكلمة سر آمنة
- إدارة جلسات المستخدمين
- حماية العمليات الحساسة برموز الجلسة

### 2. **إدارة الملفات**
- رفع الملفات (PDF, DOC, DOCX, TXT)
- حذف الملفات
- تتبع عدد التحميلات
- عرض معلومات الملف (التاريخ، الحجم، الوصف)

### 3. **البحث والتصفية**
- البحث السريع عن الملفات
- التصفية حسب المعيار (ACI, ASTM, BS)
- عرض النتائج الفورية

### 4. **الإحصائيات**
- عدد الملفات لكل معيار
- إجمالي التحميلات
- معلومات تفصيلية عن الاستخدام

### 5. **واجهة عربية**
- دعم كامل للغة العربية
- اتجاه من اليمين لليسار (RTL)
- تصميم احترافي وسهل الاستخدام

---

## 🔐 نظام الأمان

### مستويات الوصول

| العملية | الزوار | المسؤول |
|--------|--------|--------|
| عرض الملفات | ✅ | ✅ |
| تحميل الملفات | ✅ | ✅ |
| البحث | ✅ | ✅ |
| الإحصائيات | ✅ | ✅ |
| رفع ملفات | ❌ | ✅ |
| حذف ملفات | ❌ | ✅ |
| تسجيل الدخول | ❌ | ✅ |

### كلمة السر الافتراضية
```
elkasaby2025
```

**⚠️ يُنصح بتغيير كلمة السر الافتراضية في `server_v2.js`**

---

## 🚀 البدء السريع

### المتطلبات
- Node.js 14+
- npm أو yarn

### التثبيت

```bash
cd elkasaby_standards_platform
npm install
```

### التشغيل

```bash
node server_v2.js
```

الخادم سيعمل على: `http://localhost:5000`

---

## 📡 واجهات API

### المصادقة

#### تسجيل الدخول
```bash
POST /api/admin/login
Content-Type: application/json

{
  "password": "elkasaby2025"
}

Response:
{
  "success": true,
  "token": "68xfboya9bc2r7np79hx6w"
}
```

#### التحقق من حالة المسؤول
```bash
GET /api/admin/status
X-Admin-Token: 68xfboya9bc2r7np79hx6w

Response:
{
  "isAdmin": true
}
```

#### تسجيل الخروج
```bash
POST /api/admin/logout
X-Admin-Token: 68xfboya9bc2r7np79hx6w

Response:
{
  "success": true
}
```

### المعايير

#### الحصول على جميع المعايير
```bash
GET /api/standards

Response:
[
  {
    "id": 1,
    "code": "ACI",
    "name": "American Concrete Institute",
    "icon": "🇺🇸",
    "description": "..."
  },
  ...
]
```

### الملفات

#### الحصول على ملفات معيار محدد
```bash
GET /api/standards/{standardId}/files

Response:
[
  {
    "id": 1,
    "title": "شرح ACI 214R-11",
    "description": "...",
    "filename": "1731323281234-file.pdf",
    "downloads": 5,
    "uploaded_at": "2025-11-11T18:55:00Z"
  },
  ...
]
```

#### رفع ملف (مسؤول فقط)
```bash
POST /api/files/upload
X-Admin-Token: {token}
Content-Type: multipart/form-data

Parameters:
- file: (binary)
- standardId: 1
- title: "عنوان الملف"
- description: "وصف الملف"

Response:
{
  "success": true,
  "message": "تم رفع الملف بنجاح"
}
```

#### تحميل ملف
```bash
GET /api/files/{fileId}/download

Response: (Binary file data)
```

#### حذف ملف (مسؤول فقط)
```bash
DELETE /api/files/{fileId}
X-Admin-Token: {token}

Response:
{
  "success": true,
  "message": "تم حذف الملف"
}
```

### البحث

#### البحث عن ملفات
```bash
GET /api/search?query={searchTerm}

Response:
[
  {
    "id": 1,
    "title": "...",
    "description": "...",
    ...
  },
  ...
]
```

### الإحصائيات

#### الحصول على الإحصائيات
```bash
GET /api/statistics

Response:
[
  {
    "id": 1,
    "name": "ACI",
    "icon": "🇺🇸",
    "fileCount": 5,
    "totalDownloads": 25
  },
  ...
]
```

---

## 📁 هيكل المشروع

```
elkasaby_standards_platform/
├── server_v2.js              # الخادم الرئيسي
├── standards.db              # قاعدة البيانات
├── package.json              # المكتبات المطلوبة
├── public/
│   └── index.html            # الواجهة الأمامية
├── uploads/                  # مجلد الملفات المرفوعة
├── DOCUMENTATION.md          # هذا الملف
└── todo.md                   # قائمة المهام
```

---

## 🗄️ قاعدة البيانات

### جداول قاعدة البيانات

#### جدول `standards`
```sql
CREATE TABLE standards (
  id INTEGER PRIMARY KEY,
  code TEXT UNIQUE,
  name TEXT,
  icon TEXT,
  description TEXT
);
```

#### جدول `files`
```sql
CREATE TABLE files (
  id INTEGER PRIMARY KEY AUTOINCREMENT,
  standard_id INTEGER,
  title TEXT,
  description TEXT,
  filename TEXT,
  filepath TEXT,
  filesize INTEGER,
  downloads INTEGER DEFAULT 0,
  uploaded_at DATETIME DEFAULT CURRENT_TIMESTAMP,
  FOREIGN KEY (standard_id) REFERENCES standards(id)
);
```

---

## 🎨 الواجهة الأمامية

### المكونات الرئيسية

1. **رأس الصفحة**: شعار المنصة وزر تسجيل الدخول
2. **الإحصائيات**: عرض عدد الملفات والتحميلات
3. **المعايير**: بطاقات المعايير الثلاثة (ACI, ASTM, BS)
4. **نموذج الرفع**: متاح للمسؤول فقط
5. **البحث**: شريط بحث سريع
6. **قائمة الملفات**: عرض الملفات مع خيارات التحميل والحذف

---

## 🔧 التخصيص والإعدادات

### تغيير كلمة السر

في ملف `server_v2.js`، ابحث عن السطر:
```javascript
const ADMIN_PASSWORD = 'elkasaby2025';
```

وغيره إلى كلمة السر المطلوبة:
```javascript
const ADMIN_PASSWORD = 'your_new_password';
```

### تغيير المنفذ

في ملف `server_v2.js`، ابحث عن السطر:
```javascript
const PORT = 5000;
```

وغيره إلى المنفذ المطلوب.

### إضافة معايير جديدة

في ملف `server_v2.js`، في دالة `initializeDatabase()`:
```javascript
db.prepare(`
  INSERT INTO standards (code, name, icon, description)
  VALUES (?, ?, ?, ?)
`).run('NEW', 'New Standard', '🆕', 'Description');
```

---

## 🐛 استكشاف الأخطاء

### المشكلة: لا يمكن الوصول إلى الخادم
**الحل**: تأكد من تشغيل الخادم وأنك تستخدم المنفذ الصحيح

### المشكلة: لا يمكن رفع الملفات
**الحل**: تأكد من:
- تسجيل الدخول بنجاح
- نوع الملف مدعوم (PDF, DOC, DOCX, TXT)
- حجم الملف معقول

### المشكلة: الملفات المرفوعة لا تظهر
**الحل**: 
- تحقق من أن الملف تم حفظه في مجلد `uploads`
- تحقق من قاعدة البيانات

---

## 📊 الإحصائيات والمراقبة

### تتبع الاستخدام
- عدد التحميلات لكل ملف
- تاريخ الرفع
- حجم الملف

### السجلات
- يتم حفظ جميع العمليات في قاعدة البيانات
- يمكن الاستعلام عن السجلات باستخدام SQL

---

## 🚀 النشر والاستضافة

### المتطلبات
- خادم Node.js
- قاعدة بيانات SQLite (مضمنة)
- مساحة تخزين كافية للملفات

### خطوات النشر
1. انسخ المشروع إلى الخادم
2. ثبت المكتبات: `npm install`
3. شغل الخادم: `node server_v2.js`
4. استخدم reverse proxy (مثل Nginx) للوصول الآمن

---

## 📞 الدعم والمساعدة

للمزيد من المعلومات أو الإبلاغ عن مشاكل، يرجى التواصل مع محمد القصبى.

---

## 📝 الترخيص

جميع الحقوق محفوظة © 2025 محمد القصبى

---

**آخر تحديث**: 11 نوفمبر 2025
